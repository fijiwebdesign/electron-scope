console.log('hello world!')
console.log('Code to be debugged is loaded!')

/*

var Module = require('module')

 var wrapper = Module.wrap(content);

  var compiledWrapper = vm.runInThisContext(wrapper, {
    filename: filename,
    lineOffset: 0,
    displayErrors: true
  })

*/